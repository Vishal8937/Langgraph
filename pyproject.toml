[project]
name = "langraph"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vishal", email = "e23cseu0834@bennett.edu.in" }
]
requires-python = ">=3.11,<3.13"
dependencies = [
    "ipykernel>=7.3.0",
]

[project.scripts]
langraph = "langraph:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
